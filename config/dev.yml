static:
  exclude:
    - '*.phar*' # dev only
metatags:
  jsonld: false
  favicon:
    enabled: false
body:
  images:
    lazy:
      enabled: true
    caption:
      enabled: true
    remote:
      enabled: true
    resize:
      enabled: false
    responsive:
      enabled: false
    webp:
      enabled: false
  highlight:
    enabled: true
