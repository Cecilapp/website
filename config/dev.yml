static:
  exclude:
    - '*.phar*' # dev only
metatags:
  jsonld: false
body:
  images:
    lazy:
      enabled: false
    caption:
      enabled: false
    resize:
      enabled: false
    responsive:
      enabled: false
    webp:
      enabled: false
