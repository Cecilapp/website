{% extends "default.html.twig" %}

{% block title %}
<h1>{{ page.title }}</h1>
{% endblock title %}
{% block content %}
<ol>
{% for item in page.pages|sortByDate %}
<li><a href="{{ url(item) }}">{{ item.title|slice(2) }}</a></li>
{% endfor %}
</ol>
{% endblock content %}
