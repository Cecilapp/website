{#-
  FlexSearch index, consumed client-side by `partials/search-flexsearch.html.twig`.
  The extraction logic is shared with the Algolia index, see
  `partials/search-index.json.twig`.
-#}
{{- include('partials/search-index.json.twig', {id_key: 'id', with_date: false}, with_context: false) -}}
