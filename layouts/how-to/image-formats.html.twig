{% extends 'page.html.twig' %}

              {%- block content ~%}
              <h1>{{ page.title }}</h1>
              {{ page.content }}
              <h3>Live example</h3>
              {{ html(asset('images/cecil-logo.png'), attributes={alt: 'Cecil logo'}, options={responsive: false, formats: ['avif', 'webp']}) }}
              {%- endblock ~%}