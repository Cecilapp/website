    {# anchor.js #}
    {#- <script src="{{ url('js/anchor.min.js') }}"></script> ~#}
    <script src="{{ asset('https://cdnjs.cloudflare.com/ajax/libs/anchor-js/' ~ site.anchor.version ~ '/anchor.min.js',{minify:false})|url }}"></script>
    <script>{% apply minify_js %}
      var selector = '.prose > h2, .prose > h3, .prose > h4';
      anchors.options = {
        placement: 'left',
        class: 'anchor-link'
      };
      anchors.add(selector);
    {% endapply %}</script>
