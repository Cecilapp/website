            <div class="shell-wrap">
              <p class="shell-top-bar">/Users/aligny</p>
              <div class="shell-body">
                <video id="video-demo" autoplay loop muted poster="{{ asset('videos/cecil-demo-poster.png') }}" width="100%" fetchpriority="high">
                  <source data-src="{{ asset('videos/cecil-demo.mp4') }}" type="video/mp4">
                </video>
                <script>{% apply minify_js ~%}
                  // Load video only on md+ screens
                  loadVideo();
                  function loadVideo() {
                    if (window.matchMedia("(min-width: 768px)").matches) {
                      console.log('Loading video');
                      var source = document.getElementById('video-demo').getElementsByTagName('source')[0];
                      source.src = source.getAttribute('data-src');
                      document.getElementById('video-demo').load();
                    }
                  }
                  window.addEventListener('resize', loadVideo, false);
                {% endapply %}</script>
                <noscript>
                  <style>#video-demo{display:none;}</style>
                  <video autoplay loop muted poster="{{ asset('videos/cecil-demo-poster.png') }}" width="100%" fetchpriority="high">
                    <source src="{{ asset('videos/cecil-demo.mp4') }}" type="video/mp4">
                    <p>Your browser doesn't support HTML5 video. Here is a <a href="{{ url(asset('videos/cecil-demo.mp4')) }}">link to the video</a> instead.</p>
                  </video>
                </noscript>
              </div>
            </div>