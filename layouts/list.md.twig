{% set pages = site.pages.showable|filter_by('section', page.section)|sort_by_weight %}

{% extends 'page.md.twig' %}

{%- block content %}
# {{ page.title }}

> {{ page.body }}

{% for item in pages %}
- [{{ item.title }}]({{ url(item, {format: 'md', canonical: true}) }})
{% endfor %}
{% endblock %}